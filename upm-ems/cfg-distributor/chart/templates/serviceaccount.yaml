{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "distributor.serviceAccountName" . }}
  labels:
    {{- include "distributor.labels" . | nindent 4 }}
{{- end }}
