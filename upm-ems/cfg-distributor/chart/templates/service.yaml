apiVersion: v1
kind: Service
metadata:
  name: {{ include "distributor.fullname" . }}
  labels:
    {{- include "distributor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "distributor.selectorLabels" . | nindent 4 }}
