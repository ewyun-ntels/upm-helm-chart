apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: tcp-bridge-recording-rules
  namespace: upm-app
  labels:
    release: kube-prometheus-stack
    app.kubernetes.io/name: tcp-bridge
spec:
  groups:
    - name: tcp-bridge.recording.connectivity
      interval: 30s
      rules:
        - record: tcp_bridge:ready_connection_state:max
          expr: |
            label_replace(
              max by (job, namespace, pod, connection_id) (tcp_bridge_connection_state),
              "sys_id", "$1", "pod", ".*(tcp-bridge-[0-9]+).*"
            )

    - name: tcp-bridge.recording.inbound
      interval: 30s
      rules:
        - record: tcp_bridge:inbound_outcomes_total:increase2m
          expr: |
            label_replace(
              sum by (job, namespace, pod, connection_id) (increase(tcp_bridge_inbound_outcomes_total[2m])),
              "sys_id", "$1", "pod", ".*(tcp-bridge-[0-9]+).*"
            )

        - record: tcp_bridge:inbound_error_ratio:2m
          expr: |
            label_replace(
              (
                sum by (job, namespace, pod, connection_id) (increase(tcp_bridge_inbound_outcomes_total{status=~"error|timeout|dropped"}[2m]))
                /
                clamp_min(sum by (job, namespace, pod, connection_id) (increase(tcp_bridge_inbound_outcomes_total[2m])), 1)
              ),
              "sys_id", "$1", "pod", ".*(tcp-bridge-[0-9]+).*"
            )

    - name: tcp-bridge.recording.outbound
      interval: 30s
      rules:
        - record: tcp_bridge:outbound_outcomes_total:increase2m
          expr: |
            label_replace(
              sum by (job, namespace, pod, connection_id) (increase(tcp_bridge_outbound_outcomes_total[2m])),
              "sys_id", "$1", "pod", ".*(tcp-bridge-[0-9]+).*"
            )

        - record: tcp_bridge:outbound_error_ratio:2m
          expr: |
            label_replace(
              (
                sum by (job, namespace, pod, connection_id) (increase(tcp_bridge_outbound_outcomes_total{status=~"error|timeout|dropped"}[2m]))
                /
                clamp_min(sum by (job, namespace, pod, connection_id) (increase(tcp_bridge_outbound_outcomes_total[2m])), 1)
              ),
              "sys_id", "$1", "pod", ".*(tcp-bridge-[0-9]+).*"
            )
