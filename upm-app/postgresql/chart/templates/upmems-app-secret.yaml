{{- if .Values.upmems.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: upmems-app-secret
  namespace: {{ .Release.Namespace }}
  labels:
    cnpg.io/reload: "true"
type: kubernetes.io/basic-auth
stringData:
  username: {{ .Values.upmems.auth.username | quote }}
  password: {{ .Values.upmems.auth.password | quote }}
{{- end }}
